# Frontend (placeholder)

The Angular UI lands in **Phase 1, Week 6** of `BUILD_PLAN.md` — a deliberately static dashboard (workflow list, instance detail, step traces, retry button). Generative UI is deferred entirely.

To bootstrap when ready:

```bash
npx -p @angular/cli@latest ng new frontend --directory . --routing --style=scss --skip-git
```
